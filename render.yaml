services:
  - type: web
    name: videochat-server
    env: node
    plan: free
    buildCommand: |
      cd client
      npm install
      npm run build
      cd ../server
      npm install
    startCommand: node server.js
    rootDir: server
